// =============================================================
// pending_attendance_dao.dart  ->  ALGORITHM ONLY
// (source: frontend/lib/core/database/dao/pending_attendance_dao.dart)
// =============================================================

// class PendingAttendanceDao (wraps the pending_attendance table) :
//   insert(record)          -> add one offline attendance row
//   getUnsynced()           -> select all rows where synced = 0
//   markAsSynced(id)        -> set synced = 1 for that row
//   delete(id)              -> remove that row
