// =============================================================
// biometric_service.dart  ->  ALGORITHM ONLY (source: frontend/lib/core/services/biometric_service.dart)
// =============================================================

// class BiometricService (wraps local_auth) :
//   isBiometricAvailable() -> can check biometrics OR device supported (pin/pattern fallback)
//   authenticate()         -> fingerprint/face prompt, biometricOnly = true; any platform error -> false
//   getAvailableBiometrics() -> list what biometrics exist on the device
