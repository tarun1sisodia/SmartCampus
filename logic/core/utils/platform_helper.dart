// =============================================================
// platform_helper.dart  ->  ALGORITHM ONLY (source: frontend/lib/core/utils/platform_helper.dart)
// =============================================================

// class PlatformHelper :
//   isAndroid -> true only when native + android (never on web)
//   isIOS     -> true only when native + ios
//   isMobile  -> isAndroid || isIOS
//   isLinux   -> true only when native + linux
//   used everywhere to skip firebase/background tasks on web/desktop
