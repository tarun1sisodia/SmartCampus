// =============================================================
// validators.dart  ->  ALGORITHM ONLY (source: frontend/lib/core/utils/validators.dart)
// =============================================================

// class Validators :
//   static String? validateEmail(value)    -> empty = error; must match email regex; else null (valid)
//   static String? validatePassword(value) -> empty = error; length < 6 = error; must contain letter + number; else null
//   return null means "valid", a string means the error message to show under the field
