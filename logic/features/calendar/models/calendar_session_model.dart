// =============================================================
// calendar_session_model.dart  ->  ALGORITHM ONLY
// (source: frontend/lib/features/calendar/models/calendar_session_model.dart)
// =============================================================

// class CalendarSessionModel (Equatable) :
//   fields: id, subjectName, section, startTime, endTime?
//   fromJson -> mapping with key fallbacks (id/_id, subject.name...), parse datetimes
