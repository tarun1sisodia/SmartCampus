// =============================================================
// ui_feedback_mixin.dart  ->  ALGORITHM ONLY (source: frontend/lib/features/mixins/ui_feedback_mixin.dart)
// =============================================================

// mixin UIFeedbackMixin :
//   showSnackBar(context, message, isError) -> floating snackbar, red bg on error
//   showLoadingDialog(context, message)     -> non-dismissible dialog with spinner + text
//   hideLoadingDialog(context)              -> pop only if Navigator.canPop
