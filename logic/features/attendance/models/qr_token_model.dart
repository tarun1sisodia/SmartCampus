// =============================================================
// qr_token_model.dart  ->  ALGORITHM ONLY
// (source: frontend/lib/features/attendance/models/qr_token_model.dart)
// =============================================================

// class QrTokenResponse :
//   fields: token (string shown inside the QR), expiresIn (seconds until it rotates)
//   fromJson -> direct mapping
