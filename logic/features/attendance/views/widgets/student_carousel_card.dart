// =============================================================
// student_carousel_card.dart  ->  ALGORITHM ONLY
// (source: frontend/lib/features/attendance/views/widgets/student_carousel_card.dart)
// =============================================================

// class StudentCarouselCard(student, onStatusSelected, onStudentTap) :
//   build -> large card: StudentAvatar/photo, name, roll number (tap -> onStudentTap)
//   3 status buttons PRESENT / ABSENT / LATE
//     tap -> highlight the button and call onStatusSelected(status)
