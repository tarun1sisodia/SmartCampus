// =============================================================
// course_model.dart  ->  ALGORITHM ONLY (source: frontend/lib/shared/models/course_model.dart)
// =============================================================

// class CourseModel (Equatable) : id, name, code, durationYears?
//   fromJson -> _normalizeJson (id <- id|_id, name/code fallbacks) then map fields
//   toJson   -> direct field mapping
