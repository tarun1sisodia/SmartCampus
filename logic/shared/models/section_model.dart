// =============================================================
// section_model.dart  ->  ALGORITHM ONLY (source: frontend/lib/shared/models/section_model.dart)
// =============================================================

// class SectionModel (Equatable) : id, name, courseId, semesterId, classTeacherId?
//   fromJson -> normalize (id/_id, nested object fallbacks like course.id) -> map ; toJson direct
