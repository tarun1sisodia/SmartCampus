// =============================================================
// Makefile (root)  ->  ALGORITHM ONLY (source: Makefile)
// =============================================================

// SOURCE FILE IS EMPTY (placeholder only).
// Intended purpose: shortcut targets like `make dev`, `make seed`, `make backup`
//   wrapping the npm workspace scripts.
