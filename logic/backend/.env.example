// =============================================================
// .env.example  ->  ALGORITHM ONLY (source: backend/.env.example)
// =============================================================

// template of required environment keys (no real values):
//   NODE_ENV, PORT, MONGO_URI, REDIS_URL/REDIS_URL2,
//   JWT_ACCESS_SECRET(>=32), JWT_REFRESH_SECRET(>=32), JWT_ACCESS_TTL, QR_SECRET_KEY(>=24),
//   FRONTEND_URL (comma separated origins), SMTP_*, CLOUDINARY_*, AWS_* (R2 + backup bucket),
//   SENTRY_DSN, FCM_SERVICE_ACCOUNT_PATH, QR_GEOFENCE_LAT/LON/RADIUS_METERS
