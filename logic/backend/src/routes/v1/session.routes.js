// =============================================================
// session.routes.js  ->  ALGORITHM ONLY (source: backend/src/routes/v1/session.routes.js)
// =============================================================

// GET /          auth + orgScope -> list sessions (?date=today | ?month=YYYY-MM)
// GET /:sessionId auth + orgScope -> session detail + its attendance records
