// =============================================================
// index.js (routes/v1)  ->  ALGORITHM ONLY (source: backend/src/routes/v1/index.js)
// =============================================================

// mount every domain router under /api/v1:
//   /auth, /users, /students, /attendance, /session, /analytics,
//   /orgs, /notifications, /feedback, /backup, /health
