// =============================================================
// analytics.routes.js  ->  ALGORITHM ONLY (source: backend/src/routes/v1/analytics.routes.js)
// =============================================================

// GET /class/:courseId    auth + orgScope -> class report
// GET /student/:studentId auth + orgScope -> student trend
// GET /teacher/:teacherId auth + rbac(super,org_admin,teacher) + orgScope -> teacher performance
