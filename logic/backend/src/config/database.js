// =============================================================
// database.js  ->  ALGORITHM ONLY (source: backend/src/config/database.js)
// =============================================================

// connectDB() :
//   mongoose.connect(MONGO_URI || mongodb://localhost:27017/smartcampus)
//   success -> log 'MongoDB connected'
//   failure -> log error + process.exit(1)  (never run the API without a DB)
