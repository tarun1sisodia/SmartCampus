// =============================================================
// Session.model.js  ->  ALGORITHM ONLY (source: backend/src/models/Session.model.js)
// A scheduled class: the unit attendance hangs off.
// =============================================================

// schema: subject/course/semester/section/teacher refs (all required), date,
//   startTime + endTime as 'HH:mm' strings, topic, isHoliday default false,
//   qrEnabled default false, qrExpiresAt, organisation (tenant key)
