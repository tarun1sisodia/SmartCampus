// =============================================================
// sentry.js  ->  ALGORITHM ONLY (source: backend/src/utils/sentry.js)
// =============================================================

// Sentry.init({ dsn from SENTRY_DSN, environment from NODE_ENV, tracesSampleRate 1.0 })
// export { Sentry } -> reused by errorHandler to capture 5xx exceptions
