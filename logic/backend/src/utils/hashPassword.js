// =============================================================
// hashPassword.js  ->  ALGORITHM ONLY (source: backend/src/utils/hashPassword.js)
// =============================================================

// hashPassword(plain)    -> bcrypt.hash with 10 salt rounds
// comparePassword(p, h)  -> bcrypt.compare
