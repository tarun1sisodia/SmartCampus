// =============================================================
// apiResponse.js  ->  ALGORITHM ONLY (source: backend/src/utils/apiResponse.js)
// =============================================================

// sendSuccess(res, data, status=200) -> res.json { success: true, data }
// sendError(res, message, status=500) -> res.json { success: false, message }
// (uniform response envelope for the whole API)
