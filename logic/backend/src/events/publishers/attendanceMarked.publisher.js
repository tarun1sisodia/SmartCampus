// =============================================================
// attendanceMarked.publisher.js  ->  ALGORITHM ONLY (source: backend/src/events/publishers/attendanceMarked.publisher.js)
// =============================================================

// publishAttendanceMarked(data) -> eventBus.publish('attendance.marked', { sessionId, studentId, newStatus, oldStatus })
