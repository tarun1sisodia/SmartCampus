// =============================================================
// organisation.service.js  ->  ALGORITHM ONLY (source: backend/src/services/organisation.service.js)
// =============================================================

// createOrganisation(data, superAdminId) : duplicate domain -> error; stamp createdBy; create
// listOrganisations(filters, page, limit) : status/type filter + pagination
// suspendOrganisation(orgId) : 404 missing -> set status 'suspended' + save
