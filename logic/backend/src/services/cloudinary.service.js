// =============================================================
// cloudinary.service.js  ->  ALGORITHM ONLY (source: backend/src/services/cloudinary.service.js)
// =============================================================

// config from CLOUDINARY_URL (or cloud name/key/secret env)
// uploadImage(buffer, folder, publicId) -> upload_stream -> resolve secure_url (overwrite same publicId)
// deleteImage(publicId) -> cloudinary destroy
