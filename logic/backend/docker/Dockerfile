// =============================================================
// docker/Dockerfile  ->  ALGORITHM ONLY (source: backend/docker/Dockerfile)
// =============================================================

// docker/ variant of the backend image build (same install -> run flow)
