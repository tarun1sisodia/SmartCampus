// =============================================================
// logger.dart  ->  ALGORITHM ONLY (source: frontend/lib/common/utils/logging/logger.dart)
// =============================================================

// class TLogger (private constructor, one static Logger instance, debug level, pretty printer) :
//   debug(message) / info(message) / warning(message) -> forward to logger level d/i/w
//   erro(message, [error]) -> logger.e with current stack trace
