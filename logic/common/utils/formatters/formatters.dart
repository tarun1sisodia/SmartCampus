// =============================================================
// formatters.dart  ->  ALGORITHM ONLY (source: frontend/lib/common/utils/formatters/formatters.dart)
// =============================================================

// class Formatters (private constructor, static only) :
//   formatDate(date?)            -> 'dd-MM-yyyy' using intl (null -> today)
//   formatCurrency(amount)       -> US dollar currency format
//   formatPhoneNumber(phone)     -> 10 digits: '(XXX) XXX XXXX'; 11 digits similar; else unchanged
