// =============================================================
// device_utility.dart  ->  ALGORITHM ONLY (source: frontend/lib/common/utils/device/device_utility.dart)
// =============================================================

// class DeviceUtility : static helpers around device info
//   e.g. screen size, pixel ratio, padding (safe area), platform checks — all read from MediaQuery/Platform
//   pure getters, no state
