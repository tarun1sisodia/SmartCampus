// =============================================================
// snackbar_helper.dart  ->  ALGORITHM ONLY (source: frontend/lib/common/utils/helpers/snackbar_helper.dart)
// =============================================================

// enum MessageType   { success, error, warning, info }
// enum MessageSource { where the snackbar is fired from }

// class TSnackBar :
//   static show(context, message, {type, source}) :
//     pick icon + background color per MessageType
//     build a styled SnackBar and show it via ScaffoldMessenger
//     only presentation logic
