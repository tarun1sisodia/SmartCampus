// =============================================================
// default.conf (nginx)  ->  ALGORITHM ONLY (source: docker/nginx/default.conf)
// =============================================================

// SOURCE FILE IS EMPTY (placeholder only).
// Intended purpose: reverse-proxy rules — route /api to the backend container
//   and serve the web portals (TLS termination, gzip, security headers).
