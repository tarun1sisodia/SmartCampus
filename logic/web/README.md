// =============================================================
// web/ folder map  ->  ALGORITHM ONLY
// =============================================================

// web/org-admin    -> organisation admin portal (Next.js, port 3000): login, invite/deactivate
//                     teachers, CSV student import, placeholder analytics pages
// web/super-admin  -> platform super-admin portal (Next.js, port 3001): organisations CRUD +
//                     suspend, user management (roles/reset/deactivate), audit-logs & backups (placeholders)
// (per-file twins live in logic/web/org-admin/** and logic/web/super-admin/**;
//  .gitignore / .gitkeep files contain no logic and have no twins)
