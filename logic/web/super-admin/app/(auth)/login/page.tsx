// =============================================================
// (auth)/login/page.tsx (super-admin)  ->  ALGORITHM ONLY
// (source: web/super-admin/app/(auth)/login/page.tsx)
// =============================================================

// client login form (email + password):
//   dispatch(login) -> on success setSessionFlagCookie + router.push('/dashboard')
//   failure -> error toast ; loading spinner on submit
