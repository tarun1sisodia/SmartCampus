// =============================================================
// (dashboard)/organisations/[id]/page.tsx (super-admin)  ->  ALGORITHM ONLY
// (source: web/super-admin/app/(dashboard)/organisations/[id]/page.tsx)
// =============================================================

// useQuery getOrganisationById(params.id) -> detail view
// edit form -> updateOrganisation mutation -> refetch
// (pre-filled OrganisationForm, same zod validation)
