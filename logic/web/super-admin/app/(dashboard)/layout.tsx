// =============================================================
// (dashboard)/layout.tsx (super-admin)  ->  ALGORITHM ONLY
// (source: web/super-admin/app/(dashboard)/layout.tsx)
// =============================================================

// shell: Sidebar + Header (user info + logout) + children — layout only
