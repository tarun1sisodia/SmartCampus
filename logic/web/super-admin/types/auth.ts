// =============================================================
// types/auth.ts (super-admin)  ->  ALGORITHM ONLY (source: web/super-admin/types/auth.ts)
// =============================================================

// type AuthUser { id,name,email,role } ; AuthTokens ; LoginRequest ; LoginResponse — shapes only
