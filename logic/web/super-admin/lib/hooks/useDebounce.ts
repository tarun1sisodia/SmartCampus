// =============================================================
// useDebounce.ts (super-admin)  ->  ALGORITHM ONLY (source: web/super-admin/lib/hooks/useDebounce.ts)
// =============================================================

// useDebounce(value, delay=350) :
//   return a copy of value that only updates after it stopped changing for `delay` ms
//   (timer reset on every change; cleanup on unmount) — used for the org search box
