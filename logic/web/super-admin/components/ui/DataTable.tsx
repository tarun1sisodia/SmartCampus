// =============================================================
// DataTable.tsx (super-admin)  ->  ALGORITHM ONLY (source: web/super-admin/components/ui/DataTable.tsx)
// =============================================================

// generic table: columns config { key, label, render? } + rows + loading skeleton + empty state
// (presentational; pagination controls passed in)
