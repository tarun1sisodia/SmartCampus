// =============================================================
// auth-api.ts (org-admin)  ->  ALGORITHM ONLY (source: web/org-admin/lib/api/endpoints/auth-api.ts)
// =============================================================

// loginApi(payload)  -> POST /auth/login -> unwrap { accessToken, refreshToken, user }
// logoutApi()        -> POST /auth/logout
// refreshTokenApi(r) -> POST /auth/refresh -> { accessToken }
