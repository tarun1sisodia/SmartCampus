// =============================================================
// app/layout.tsx (org-admin)  ->  ALGORITHM ONLY (source: web/org-admin/app/layout.tsx)
// =============================================================

// root layout: html + body with globals.css, wrap children in AppProviders (redux + react-query + toast)
