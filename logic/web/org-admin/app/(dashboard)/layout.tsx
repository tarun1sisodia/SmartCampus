// =============================================================
// (dashboard)/layout.tsx (org-admin)  ->  ALGORITHM ONLY
// (source: web/org-admin/app/(dashboard)/layout.tsx)
// =============================================================

// dashboard shell: Sidebar + main content area (children) — layout only
