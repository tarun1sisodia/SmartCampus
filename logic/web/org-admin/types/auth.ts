// =============================================================
// types/auth.ts (org-admin)  ->  ALGORITHM ONLY (source: web/org-admin/types/auth.ts)
// =============================================================

// type AuthUser { id,name,email,role } ; AuthTokens { accessToken, refreshToken }
// LoginRequest { email, password } ; LoginResponse = tokens + user  — shapes only
