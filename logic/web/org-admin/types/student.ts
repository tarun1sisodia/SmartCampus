// =============================================================
// types/student.ts (org-admin)  ->  ALGORITHM ONLY (source: web/org-admin/types/student.ts)
// =============================================================

// type StudentImportResult { total, succeeded, failed, errors[] } — shape only
