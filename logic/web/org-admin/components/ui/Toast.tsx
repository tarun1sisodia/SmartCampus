// =============================================================
// Toast.tsx (org-admin)  ->  ALGORITHM ONLY (source: web/org-admin/components/ui/Toast.tsx)
// =============================================================

// ToastProvider : showToast(message, type) -> push { id, message, type }
//   auto-remove after timeout ; render stacked dark toasts (TYPE: message)
// useToast() hook -> context access
