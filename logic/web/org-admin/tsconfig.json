// =============================================================
// tsconfig.json (org-admin)  ->  ALGORITHM ONLY (source: web/org-admin/tsconfig.json)
// =============================================================

// TS compiler options + '@/*' path alias to project root — config only
